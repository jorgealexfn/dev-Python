#Diferença de tupla para lista: 
#lista é mutavel e tupla não é mutavel

fruit = ("orange", "pear", "grape",)

letter = tuple("python")

numbers = tuple([1, 2, 3, 4])

country = ("Brasil",)

#tuplas de tuplas -- aninhadas

